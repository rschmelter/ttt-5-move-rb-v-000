#!/usr/bin/env ruby

require_relative '../lib/move.rb'

prints "Wecome to Tic Tac Toe!"

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

prints "Where would you like to go?"

input = gets.strip

index = index_to_input(input)

move(board, index, X)

display_board(board)
